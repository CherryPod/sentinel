[package]
name = "tool-common"
version = "0.1.0"
edition = "2021"
description = "Shared guest-side helpers for Sentinel WASM tools"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
