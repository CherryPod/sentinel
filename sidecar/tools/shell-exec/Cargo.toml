[package]
name = "tool-shell-exec"
version = "0.1.0"
edition = "2021"
description = "WASM tool: execute shell commands via host function"
license = "Apache-2.0"

[dependencies]
tool-common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
